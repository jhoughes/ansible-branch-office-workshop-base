# Checkpoint: End of Section 4.1

Snapshot of the workshop repo at the end of section 4.1. New since section 3.3:

- `playbooks/broken/broken-web-deploy.yml` — the deliberately broken playbook with five bugs that attendees diagnosed using `--check`, `--diff`, `-vvv`, `--start-at-task`, and `--step`

This checkpoint is the same as section 3.3 plus the broken playbook. The "fixed" version isn't shipped — the answer key in LAB-4.1.md describes each fix, and attendees fix the file in their own working copy. If you want a confirmed-fixed reference, see the answer key at the bottom of LAB-4.1.md.

To use this checkpoint to recover, follow the same setup as the section 3.3 checkpoint (vault setup, then run playbooks).
